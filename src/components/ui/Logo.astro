---
import logo from "@assets/logo.svg";
import { Image } from "astro:assets";

const { className } = Astro.props;
---

<Image src={logo} alt="RockyPoint.net logo" width={48} height={120} class={className} />
